// Where the frontend finds the backend API.
//
// The app is always served by the Node server itself (localhost, Docker,
// Cloudflare Tunnel), so the API lives on the same origin.
export const API_BASE = "/api";
